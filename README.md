import random

steam = [
    "NULL",  "NULL", "NULL", "NULL", "NULL", "NULL", "NULL", "NULL", "NULL", "NULL",
]

for i in range(10)
    game_add()
    
    ask = input("Would you like to add any more games?")


def game_add():
    game = input("Name a game in your steam libary!: ")
    steam[i] = game
    print(str(steam[i]) + "Has been added to your favs!")

